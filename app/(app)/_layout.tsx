import { AppNavigator } from '@/navigation/AppNavigator';

/**
 * Mount point only. See `app/(app)/people/index.tsx` for why the shell and its test live under
 * `src/navigation/` instead.
 */
export default AppNavigator;
