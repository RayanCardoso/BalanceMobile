import { RegisterRecurringExpenseScreen } from '@/screens/RegisterRecurringExpense/RegisterRecurringExpenseScreen';

/**
 * Mount point only. See `app/(app)/people/index.tsx` for why the screen and its test live under
 * `src/features/` instead.
 */
export default RegisterRecurringExpenseScreen;
